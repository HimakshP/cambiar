<?xml version="1.0" standalone="no"?><!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd"><svg version="1.1" width="1200" height="390" onload="init(evt)" viewBox="0 0 1200 390" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" xmlns:fg="http://github.com/jonhoo/inferno"><!--Flame graph stack visualization. See https://github.com/brendangregg/FlameGraph for latest version, and http://www.brendangregg.com/flamegraphs.html for examples.--><!--NOTES: --><defs><linearGradient id="background" y1="0" y2="1" x1="0" x2="0"><stop stop-color="#eeeeee" offset="5%"/><stop stop-color="#eeeeb0" offset="95%"/></linearGradient></defs><style type="text/css">
text { font-family:monospace; font-size:12px }
#title { text-anchor:middle; font-size:17px; }
#matched { text-anchor:end; }
#search { text-anchor:end; opacity:0.1; cursor:pointer; }
#search:hover, #search.show { opacity:1; }
#subtitle { text-anchor:middle; font-color:rgb(160,160,160); }
#unzoom { cursor:pointer; }
#frames > *:hover { stroke:black; stroke-width:0.5; cursor:pointer; }
.hide { display:none; }
.parent { opacity:0.5; }
</style><script type="text/ecmascript"><![CDATA[
        var nametype = 'Function:';
        var fontsize = 12;
        var fontwidth = 0.59;
        var xpad = 10;
        var inverted = false;
        var searchcolor = 'rgb(230,0,230)';
        var fluiddrawing = true;
        var truncate_text_right = false;
    ]]><![CDATA["use strict";
var details, searchbtn, unzoombtn, matchedtxt, svg, searching, frames, known_font_width;
function init(evt) {
    details = document.getElementById("details").firstChild;
    searchbtn = document.getElementById("search");
    unzoombtn = document.getElementById("unzoom");
    matchedtxt = document.getElementById("matched");
    svg = document.getElementsByTagName("svg")[0];
    frames = document.getElementById("frames");
    known_font_width = get_monospace_width(frames);
    total_samples = parseInt(frames.attributes.total_samples.value);
    searching = 0;

    // Use GET parameters to restore a flamegraph's state.
    var restore_state = function() {
        var params = get_params();
        if (params.x && params.y)
            zoom(find_group(document.querySelector('[*|x="' + params.x + '"][y="' + params.y + '"]')));
        if (params.s)
            search(params.s);
    };

    if (fluiddrawing) {
        // Make width dynamic so the SVG fits its parent's width.
        svg.removeAttribute("width");
        // Edge requires us to have a viewBox that gets updated with size changes.
        var isEdge = /Edge\/\d./i.test(navigator.userAgent);
        if (!isEdge) {
            svg.removeAttribute("viewBox");
        }
        var update_for_width_change = function() {
            if (isEdge) {
                svg.attributes.viewBox.value = "0 0 " + svg.width.baseVal.value + " " + svg.height.baseVal.value;
            }

            // Keep consistent padding on left and right of frames container.
            frames.attributes.width.value = svg.width.baseVal.value - xpad * 2;

            // Text truncation needs to be adjusted for the current width.
            update_text_for_elements(frames.children);

            // Keep search elements at a fixed distance from right edge.
            var svgWidth = svg.width.baseVal.value;
            searchbtn.attributes.x.value = svgWidth - xpad;
            matchedtxt.attributes.x.value = svgWidth - xpad;
        };
        window.addEventListener('resize', function() {
            update_for_width_change();
        });
        // This needs to be done asynchronously for Safari to work.
        setTimeout(function() {
            unzoom();
            update_for_width_change();
            restore_state();
        }, 0);
    } else {
        restore_state();
    }
}
// event listeners
window.addEventListener("click", function(e) {
    var target = find_group(e.target);
    if (target) {
        if (target.nodeName == "a") {
            if (e.ctrlKey === false) return;
            e.preventDefault();
        }
        if (target.classList.contains("parent")) unzoom();
        zoom(target);

        // set parameters for zoom state
        var el = target.querySelector("rect");
        if (el && el.attributes && el.attributes.y && el.attributes["fg:x"]) {
            var params = get_params()
            params.x = el.attributes["fg:x"].value;
            params.y = el.attributes.y.value;
            history.replaceState(null, null, parse_params(params));
        }
    }
    else if (e.target.id == "unzoom") {
        unzoom();

        // remove zoom state
        var params = get_params();
        if (params.x) delete params.x;
        if (params.y) delete params.y;
        history.replaceState(null, null, parse_params(params));
    }
    else if (e.target.id == "search") search_prompt();
}, false)
// mouse-over for info
// show
window.addEventListener("mouseover", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = nametype + " " + g_to_text(target);
}, false)
// clear
window.addEventListener("mouseout", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = ' ';
}, false)
// ctrl-F for search
window.addEventListener("keydown",function (e) {
    if (e.keyCode === 114 || (e.ctrlKey && e.keyCode === 70)) {
        e.preventDefault();
        search_prompt();
    }
}, false)
// functions
function get_params() {
    var params = {};
    var paramsarr = window.location.search.substr(1).split('&');
    for (var i = 0; i < paramsarr.length; ++i) {
        var tmp = paramsarr[i].split("=");
        if (!tmp[0] || !tmp[1]) continue;
        params[tmp[0]]  = decodeURIComponent(tmp[1]);
    }
    return params;
}
function parse_params(params) {
    var uri = "?";
    for (var key in params) {
        uri += key + '=' + encodeURIComponent(params[key]) + '&';
    }
    if (uri.slice(-1) == "&")
        uri = uri.substring(0, uri.length - 1);
    if (uri == '?')
        uri = window.location.href.split('?')[0];
    return uri;
}
function find_child(node, selector) {
    var children = node.querySelectorAll(selector);
    if (children.length) return children[0];
    return;
}
function find_group(node) {
    var parent = node.parentElement;
    if (!parent) return;
    if (parent.id == "frames") return node;
    return find_group(parent);
}
function orig_save(e, attr, val) {
    if (e.attributes["fg:orig_" + attr] != undefined) return;
    if (e.attributes[attr] == undefined) return;
    if (val == undefined) val = e.attributes[attr].value;
    e.setAttribute("fg:orig_" + attr, val);
}
function orig_load(e, attr) {
    if (e.attributes["fg:orig_"+attr] == undefined) return;
    e.attributes[attr].value = e.attributes["fg:orig_" + attr].value;
    e.removeAttribute("fg:orig_" + attr);
}
function g_to_text(e) {
    var text = find_child(e, "title").firstChild.nodeValue;
    return (text)
}
function g_to_func(e) {
    var func = g_to_text(e);
    // if there's any manipulation we want to do to the function
    // name before it's searched, do it here before returning.
    return (func);
}
function get_monospace_width(frames) {
    // Given the id="frames" element, return the width of text characters if
    // this is a monospace font, otherwise return 0.
    text = find_child(frames.children[0], "text");
    originalContent = text.textContent;
    text.textContent = "!";
    bangWidth = text.getComputedTextLength();
    text.textContent = "W";
    wWidth = text.getComputedTextLength();
    text.textContent = originalContent;
    if (bangWidth === wWidth) {
        return bangWidth;
    } else {
        return 0;
    }
}
function update_text_for_elements(elements) {
    // In order to render quickly in the browser, you want to do one pass of
    // reading attributes, and one pass of mutating attributes. See
    // https://web.dev/avoid-large-complex-layouts-and-layout-thrashing/ for details.

    // Fall back to inefficient calculation, if we're variable-width font.
    // TODO This should be optimized somehow too.
    if (known_font_width === 0) {
        for (var i = 0; i < elements.length; i++) {
            update_text(elements[i]);
        }
        return;
    }

    var textElemNewAttributes = [];
    for (var i = 0; i < elements.length; i++) {
        var e = elements[i];
        var r = find_child(e, "rect");
        var t = find_child(e, "text");
        var w = parseFloat(r.attributes.width.value) * frames.attributes.width.value / 100 - 3;
        var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
        var newX = format_percent((parseFloat(r.attributes.x.value) + (100 * 3 / frames.attributes.width.value)));

        // Smaller than this size won't fit anything
        if (w < 2 * known_font_width) {
            textElemNewAttributes.push([newX, ""]);
            continue;
        }

        // Fit in full text width
        if (txt.length * known_font_width < w) {
            textElemNewAttributes.push([newX, txt]);
            continue;
        }

        var substringLength = Math.floor(w / known_font_width) - 2;
        if (truncate_text_right) {
            // Truncate the right side of the text.
            textElemNewAttributes.push([newX, txt.substring(0, substringLength) + ".."]);
            continue;
        } else {
            // Truncate the left side of the text.
            textElemNewAttributes.push([newX, ".." + txt.substring(txt.length - substringLength, txt.length)]);
            continue;
        }
    }

    console.assert(textElemNewAttributes.length === elements.length, "Resize failed, please file a bug at https://github.com/jonhoo/inferno/");

    // Now that we know new textContent, set it all in one go so we don't refresh a bazillion times.
    for (var i = 0; i < elements.length; i++) {
        var e = elements[i];
        var values = textElemNewAttributes[i];
        var t = find_child(e, "text");
        t.attributes.x.value = values[0];
        t.textContent = values[1];
    }
}

function update_text(e) {
    var r = find_child(e, "rect");
    var t = find_child(e, "text");
    var w = parseFloat(r.attributes.width.value) * frames.attributes.width.value / 100 - 3;
    var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
    t.attributes.x.value = format_percent((parseFloat(r.attributes.x.value) + (100 * 3 / frames.attributes.width.value)));

    // Smaller than this size won't fit anything
    if (w < 2 * fontsize * fontwidth) {
        t.textContent = "";
        return;
    }
    t.textContent = txt;
    // Fit in full text width
    if (t.getComputedTextLength() < w)
        return;
    if (truncate_text_right) {
        // Truncate the right side of the text.
        for (var x = txt.length - 2; x > 0; x--) {
            if (t.getSubStringLength(0, x + 2) <= w) {
                t.textContent = txt.substring(0, x) + "..";
                return;
            }
        }
    } else {
        // Truncate the left side of the text.
        for (var x = 2; x < txt.length; x++) {
            if (t.getSubStringLength(x - 2, txt.length) <= w) {
                t.textContent = ".." + txt.substring(x, txt.length);
                return;
            }
        }
    }
    t.textContent = "";
}
// zoom
function zoom_reset(e) {
    if (e.tagName == "rect") {
        e.attributes.x.value = format_percent(100 * parseInt(e.attributes["fg:x"].value) / total_samples);
        e.attributes.width.value = format_percent(100 * parseInt(e.attributes["fg:w"].value) / total_samples);
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_reset(c[i]);
    }
}
function zoom_child(e, x, zoomed_width_samples) {
    if (e.tagName == "text") {
        var parent_x = parseFloat(find_child(e.parentNode, "rect[x]").attributes.x.value);
        e.attributes.x.value = format_percent(parent_x + (100 * 3 / frames.attributes.width.value));
    } else if (e.tagName == "rect") {
        e.attributes.x.value = format_percent(100 * (parseInt(e.attributes["fg:x"].value) - x) / zoomed_width_samples);
        e.attributes.width.value = format_percent(100 * parseInt(e.attributes["fg:w"].value) / zoomed_width_samples);
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_child(c[i], x, zoomed_width_samples);
    }
}
function zoom_parent(e) {
    if (e.attributes) {
        if (e.attributes.x != undefined) {
            e.attributes.x.value = "0.0%";
        }
        if (e.attributes.width != undefined) {
            e.attributes.width.value = "100.0%";
        }
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_parent(c[i]);
    }
}
function zoom(node) {
    var attr = find_child(node, "rect").attributes;
    var width = parseInt(attr["fg:w"].value);
    var xmin = parseInt(attr["fg:x"].value);
    var xmax = xmin + width;
    var ymin = parseFloat(attr.y.value);
    unzoombtn.classList.remove("hide");
    var el = frames.children;
    var to_update_text = [];
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        var a = find_child(e, "rect").attributes;
        var ex = parseInt(a["fg:x"].value);
        var ew = parseInt(a["fg:w"].value);
        // Is it an ancestor
        if (!inverted) {
            var upstack = parseFloat(a.y.value) > ymin;
        } else {
            var upstack = parseFloat(a.y.value) < ymin;
        }
        if (upstack) {
            // Direct ancestor
            if (ex <= xmin && (ex+ew) >= xmax) {
                e.classList.add("parent");
                zoom_parent(e);
                to_update_text.push(e);
            }
            // not in current path
            else
                e.classList.add("hide");
        }
        // Children maybe
        else {
            // no common path
            if (ex < xmin || ex >= xmax) {
                e.classList.add("hide");
            }
            else {
                zoom_child(e, xmin, width);
                to_update_text.push(e);
            }
        }
    }
    update_text_for_elements(to_update_text);
}
function unzoom() {
    unzoombtn.classList.add("hide");
    var el = frames.children;
    for(var i = 0; i < el.length; i++) {
        el[i].classList.remove("parent");
        el[i].classList.remove("hide");
        zoom_reset(el[i]);
    }
    update_text_for_elements(el);
}
// search
function reset_search() {
    var el = document.querySelectorAll("#frames rect");
    for (var i = 0; i < el.length; i++) {
        orig_load(el[i], "fill")
    }
    var params = get_params();
    delete params.s;
    history.replaceState(null, null, parse_params(params));
}
function search_prompt() {
    if (!searching) {
        var term = prompt("Enter a search term (regexp " +
            "allowed, eg: ^ext4_)", "");
        if (term != null) {
            search(term)
        }
    } else {
        reset_search();
        searching = 0;
        searchbtn.classList.remove("show");
        searchbtn.firstChild.nodeValue = "Search"
        matchedtxt.classList.add("hide");
        matchedtxt.firstChild.nodeValue = ""
    }
}
function search(term) {
    var re = new RegExp(term);
    var el = frames.children;
    var matches = new Object();
    var maxwidth = 0;
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        // Skip over frames which are either not visible, or below the zoomed-to frame
        if (e.classList.contains("hide") || e.classList.contains("parent")) {
            continue;
        }
        var func = g_to_func(e);
        var rect = find_child(e, "rect");
        if (func == null || rect == null)
            continue;
        // Save max width. Only works as we have a root frame
        var w = parseInt(rect.attributes["fg:w"].value);
        if (w > maxwidth)
            maxwidth = w;
        if (func.match(re)) {
            // highlight
            var x = parseInt(rect.attributes["fg:x"].value);
            orig_save(rect, "fill");
            rect.attributes.fill.value = searchcolor;
            // remember matches
            if (matches[x] == undefined) {
                matches[x] = w;
            } else {
                if (w > matches[x]) {
                    // overwrite with parent
                    matches[x] = w;
                }
            }
            searching = 1;
        }
    }
    if (!searching)
        return;
    var params = get_params();
    params.s = term;
    history.replaceState(null, null, parse_params(params));

    searchbtn.classList.add("show");
    searchbtn.firstChild.nodeValue = "Reset Search";
    // calculate percent matched, excluding vertical overlap
    var count = 0;
    var lastx = -1;
    var lastw = 0;
    var keys = Array();
    for (k in matches) {
        if (matches.hasOwnProperty(k))
            keys.push(k);
    }
    // sort the matched frames by their x location
    // ascending, then width descending
    keys.sort(function(a, b){
        return a - b;
    });
    // Step through frames saving only the biggest bottom-up frames
    // thanks to the sort order. This relies on the tree property
    // where children are always smaller than their parents.
    for (var k in keys) {
        var x = parseInt(keys[k]);
        var w = matches[keys[k]];
        if (x >= lastx + lastw) {
            count += w;
            lastx = x;
            lastw = w;
        }
    }
    // display matched percent
    matchedtxt.classList.remove("hide");
    var pct = 100 * count / maxwidth;
    if (pct != 100) pct = pct.toFixed(1);
    matchedtxt.firstChild.nodeValue = "Matched: " + pct + "%";
}
function format_percent(n) {
    return n.toFixed(4) + "%";
}
]]></script><rect x="0" y="0" width="100%" height="390" fill="url(#background)"/><text id="title" fill="rgb(0,0,0)" x="50.0000%" y="24.00">Flame Graph</text><text id="details" fill="rgb(0,0,0)" x="10" y="373.00"> </text><text id="unzoom" class="hide" fill="rgb(0,0,0)" x="10" y="24.00">Reset Zoom</text><text id="search" fill="rgb(0,0,0)" x="1190" y="24.00">Search</text><text id="matched" fill="rgb(0,0,0)" x="1190" y="373.00"> </text><svg id="frames" x="10" width="1180" total_samples="34830972"><g><title>_int_realloc (2,495,564 samples, 7.16%)</title><rect x="0.0000%" y="293" width="7.1648%" height="15" fill="rgb(227,0,7)" fg:x="0" fg:w="2495564"/><text x="0.2500%" y="303.50">_int_reall..</text></g><g><title>finish_list (2,495,564 samples, 7.16%)</title><rect x="0.0000%" y="277" width="7.1648%" height="15" fill="rgb(217,0,24)" fg:x="0" fg:w="2495564"/><text x="0.2500%" y="287.50">finish_list</text></g><g><title>index&lt;pulldown_cmark::parse::Item&gt; (2,495,564 samples, 7.16%)</title><rect x="0.0000%" y="261" width="7.1648%" height="15" fill="rgb(221,193,54)" fg:x="0" fg:w="2495564"/><text x="0.2500%" y="271.50">index&lt;pull..</text></g><g><title>index&lt;pulldown_cmark::tree::Node&lt;pulldown_cmark::parse::Item&gt;, usize, alloc::alloc::Global&gt; (2,495,564 samples, 7.16%)</title><rect x="0.0000%" y="245" width="7.1648%" height="15" fill="rgb(248,212,6)" fg:x="0" fg:w="2495564"/><text x="0.2500%" y="255.50">index&lt;pull..</text></g><g><title>index&lt;pulldown_cmark::tree::Node&lt;pulldown_cmark::parse::Item&gt;, usize&gt; (2,495,564 samples, 7.16%)</title><rect x="0.0000%" y="229" width="7.1648%" height="15" fill="rgb(208,68,35)" fg:x="0" fg:w="2495564"/><text x="0.2500%" y="239.50">index&lt;pull..</text></g><g><title>index&lt;pulldown_cmark::tree::Node&lt;pulldown_cmark::parse::Item&gt;&gt; (2,495,564 samples, 7.16%)</title><rect x="0.0000%" y="213" width="7.1648%" height="15" fill="rgb(232,128,0)" fg:x="0" fg:w="2495564"/><text x="0.2500%" y="223.50">index&lt;pull..</text></g><g><title>append&lt;pulldown_cmark::parse::Item&gt; (2,607,104 samples, 7.49%)</title><rect x="7.1648%" y="293" width="7.4850%" height="15" fill="rgb(207,160,47)" fg:x="2495564" fg:w="2607104"/><text x="7.4148%" y="303.50">append&lt;pul..</text></g><g><title>last&lt;pulldown_cmark::tree::TreeIndex&gt; (2,607,104 samples, 7.49%)</title><rect x="7.1648%" y="277" width="7.4850%" height="15" fill="rgb(228,23,34)" fg:x="2495564" fg:w="2607104"/><text x="7.4148%" y="287.50">last&lt;pulld..</text></g><g><title>deref (501,504 samples, 1.44%)</title><rect x="14.6498%" y="293" width="1.4398%" height="15" fill="rgb(218,30,26)" fg:x="5102668" fg:w="501504"/><text x="14.8998%" y="303.50"></text></g><g><title>finish_list_marker (1,137,408 samples, 3.27%)</title><rect x="16.0896%" y="293" width="3.2655%" height="15" fill="rgb(220,122,19)" fg:x="5604172" fg:w="1137408"/><text x="16.3396%" y="303.50">fin..</text></g><g><title>scan_space_upto (1,137,408 samples, 3.27%)</title><rect x="16.0896%" y="277" width="3.2655%" height="15" fill="rgb(250,228,42)" fg:x="5604172" fg:w="1137408"/><text x="16.3396%" y="287.50">sca..</text></g><g><title>scan_space_inner (1,137,408 samples, 3.27%)</title><rect x="16.0896%" y="261" width="3.2655%" height="15" fill="rgb(240,193,28)" fg:x="5604172" fg:w="1137408"/><text x="16.3396%" y="271.50">sca..</text></g><g><title>parse_block (2,884,861 samples, 8.28%)</title><rect x="19.3551%" y="293" width="8.2825%" height="15" fill="rgb(216,20,37)" fg:x="6741580" fg:w="2884861"/><text x="19.6051%" y="303.50">parse_block</text></g><g><title>scan_space_upto (2,383,357 samples, 6.84%)</title><rect x="20.7950%" y="277" width="6.8426%" height="15" fill="rgb(206,188,39)" fg:x="7243084" fg:w="2383357"/><text x="21.0450%" y="287.50">scan_spac..</text></g><g><title>parse_paragraph (2,732,379 samples, 7.84%)</title><rect x="27.6376%" y="293" width="7.8447%" height="15" fill="rgb(217,207,13)" fg:x="9626441" fg:w="2732379"/><text x="27.8876%" y="303.50">parse_parag..</text></g><g><title>push&lt;pulldown_cmark::parse::Item&gt; (2,732,379 samples, 7.84%)</title><rect x="27.6376%" y="277" width="7.8447%" height="15" fill="rgb(231,73,38)" fg:x="9626441" fg:w="2732379"/><text x="27.8876%" y="287.50">push&lt;pulldo..</text></g><g><title>push&lt;pulldown_cmark::tree::TreeIndex, alloc::alloc::Global&gt; (2,732,379 samples, 7.84%)</title><rect x="27.6376%" y="261" width="7.8447%" height="15" fill="rgb(225,20,46)" fg:x="9626441" fg:w="2732379"/><text x="27.8876%" y="271.50">push&lt;pulldo..</text></g><g><title>push_mut&lt;pulldown_cmark::tree::TreeIndex, alloc::alloc::Global&gt; (2,732,379 samples, 7.84%)</title><rect x="27.6376%" y="245" width="7.8447%" height="15" fill="rgb(210,31,41)" fg:x="9626441" fg:w="2732379"/><text x="27.8876%" y="255.50">push_mut&lt;pu..</text></g><g><title>pop (1,133,761 samples, 3.26%)</title><rect x="35.4823%" y="293" width="3.2550%" height="15" fill="rgb(221,200,47)" fg:x="12358820" fg:w="1133761"/><text x="35.7323%" y="303.50">pop</text></g><g><title>scan_containers (2,800,917 samples, 8.04%)</title><rect x="38.7373%" y="293" width="8.0415%" height="15" fill="rgb(226,26,5)" fg:x="13492581" fg:w="2800917"/><text x="38.9873%" y="303.50">scan_contai..</text></g><g><title>scan_list_marker_with_indent (3,309,333 samples, 9.50%)</title><rect x="46.7788%" y="293" width="9.5011%" height="15" fill="rgb(249,33,26)" fg:x="16293498" fg:w="3309333"/><text x="47.0288%" y="303.50">scan_list_mark..</text></g><g><title>[unknown] (21,157,190 samples, 60.74%)</title><rect x="0.0000%" y="309" width="60.7425%" height="15" fill="rgb(235,183,28)" fg:x="0" fg:w="21157190"/><text x="0.2500%" y="319.50">[unknown]</text></g><g><title>scan_listitem (1,554,359 samples, 4.46%)</title><rect x="56.2799%" y="293" width="4.4626%" height="15" fill="rgb(221,5,38)" fg:x="19602831" fg:w="1554359"/><text x="56.5299%" y="303.50">scan_..</text></g><g><title>calc_indent (1,554,359 samples, 4.46%)</title><rect x="56.2799%" y="277" width="4.4626%" height="15" fill="rgb(247,18,42)" fg:x="19602831" fg:w="1554359"/><text x="56.5299%" y="287.50">calc_..</text></g><g><title>_start (501,504 samples, 1.44%)</title><rect x="60.7425%" y="309" width="1.4398%" height="15" fill="rgb(241,131,45)" fg:x="21157190" fg:w="501504"/><text x="60.9925%" y="319.50"></text></g><g><title>__libc_start_main@@GLIBC_2.34 (501,504 samples, 1.44%)</title><rect x="60.7425%" y="293" width="1.4398%" height="15" fill="rgb(249,31,29)" fg:x="21157190" fg:w="501504"/><text x="60.9925%" y="303.50"></text></g><g><title>main (501,504 samples, 1.44%)</title><rect x="60.7425%" y="277" width="1.4398%" height="15" fill="rgb(225,111,53)" fg:x="21157190" fg:w="501504"/><text x="60.9925%" y="287.50"></text></g><g><title>lang_start_internal (501,504 samples, 1.44%)</title><rect x="60.7425%" y="261" width="1.4398%" height="15" fill="rgb(238,160,17)" fg:x="21157190" fg:w="501504"/><text x="60.9925%" y="271.50"></text></g><g><title>catch_unwind&lt;std::rt::lang_start_internal::{closure_env#0}, isize&gt; (501,504 samples, 1.44%)</title><rect x="60.7425%" y="245" width="1.4398%" height="15" fill="rgb(214,148,48)" fg:x="21157190" fg:w="501504"/><text x="60.9925%" y="255.50"></text></g><g><title>catch_unwind&lt;isize, std::rt::lang_start_internal::{closure_env#0}&gt; (501,504 samples, 1.44%)</title><rect x="60.7425%" y="229" width="1.4398%" height="15" fill="rgb(232,36,49)" fg:x="21157190" fg:w="501504"/><text x="60.9925%" y="239.50"></text></g><g><title>do_call&lt;std::rt::lang_start_internal::{closure_env#0}, isize&gt; (501,504 samples, 1.44%)</title><rect x="60.7425%" y="213" width="1.4398%" height="15" fill="rgb(209,103,24)" fg:x="21157190" fg:w="501504"/><text x="60.9925%" y="223.50"></text></g><g><title>{closure#0} (501,504 samples, 1.44%)</title><rect x="60.7425%" y="197" width="1.4398%" height="15" fill="rgb(229,88,8)" fg:x="21157190" fg:w="501504"/><text x="60.9925%" y="207.50"></text></g><g><title>init (501,504 samples, 1.44%)</title><rect x="60.7425%" y="181" width="1.4398%" height="15" fill="rgb(213,181,19)" fg:x="21157190" fg:w="501504"/><text x="60.9925%" y="191.50"></text></g><g><title>init (501,504 samples, 1.44%)</title><rect x="60.7425%" y="165" width="1.4398%" height="15" fill="rgb(254,191,54)" fg:x="21157190" fg:w="501504"/><text x="60.9925%" y="175.50"></text></g><g><title>init (501,504 samples, 1.44%)</title><rect x="60.7425%" y="149" width="1.4398%" height="15" fill="rgb(241,83,37)" fg:x="21157190" fg:w="501504"/><text x="60.9925%" y="159.50"></text></g><g><title>install_main_guard (501,504 samples, 1.44%)</title><rect x="60.7425%" y="133" width="1.4398%" height="15" fill="rgb(233,36,39)" fg:x="21157190" fg:w="501504"/><text x="60.9925%" y="143.50"></text></g><g><title>install_main_guard_linux (501,504 samples, 1.44%)</title><rect x="60.7425%" y="117" width="1.4398%" height="15" fill="rgb(226,3,54)" fg:x="21157190" fg:w="501504"/><text x="60.9925%" y="127.50"></text></g><g><title>stack_start_aligned (501,504 samples, 1.44%)</title><rect x="60.7425%" y="101" width="1.4398%" height="15" fill="rgb(245,192,40)" fg:x="21157190" fg:w="501504"/><text x="60.9925%" y="111.50"></text></g><g><title>get_stack_start (501,504 samples, 1.44%)</title><rect x="60.7425%" y="85" width="1.4398%" height="15" fill="rgb(238,167,29)" fg:x="21157190" fg:w="501504"/><text x="60.9925%" y="95.50"></text></g><g><title>__pthread_getattr_np (501,504 samples, 1.44%)</title><rect x="60.7425%" y="69" width="1.4398%" height="15" fill="rgb(232,182,51)" fg:x="21157190" fg:w="501504"/><text x="60.9925%" y="79.50"></text></g><g><title>__getdelim (501,504 samples, 1.44%)</title><rect x="60.7425%" y="53" width="1.4398%" height="15" fill="rgb(231,60,39)" fg:x="21157190" fg:w="501504"/><text x="60.9925%" y="63.50"></text></g><g><title>__libc_realloc (501,504 samples, 1.44%)</title><rect x="60.7425%" y="37" width="1.4398%" height="15" fill="rgb(208,69,12)" fg:x="21157190" fg:w="501504"/><text x="60.9925%" y="47.50"></text></g><g><title>append&lt;pulldown_cmark::parse::Item&gt; (834,501 samples, 2.40%)</title><rect x="62.1823%" y="309" width="2.3959%" height="15" fill="rgb(235,93,37)" fg:x="21658694" fg:w="834501"/><text x="62.4323%" y="319.50">ap..</text></g><g><title>create_node&lt;pulldown_cmark::parse::Item&gt; (834,501 samples, 2.40%)</title><rect x="62.1823%" y="293" width="2.3959%" height="15" fill="rgb(213,116,39)" fg:x="21658694" fg:w="834501"/><text x="62.4323%" y="303.50">cr..</text></g><g><title>push&lt;pulldown_cmark::tree::Node&lt;pulldown_cmark::parse::Item&gt;, alloc::alloc::Global&gt; (834,501 samples, 2.40%)</title><rect x="62.1823%" y="277" width="2.3959%" height="15" fill="rgb(222,207,29)" fg:x="21658694" fg:w="834501"/><text x="62.4323%" y="287.50">pu..</text></g><g><title>push_mut&lt;pulldown_cmark::tree::Node&lt;pulldown_cmark::parse::Item&gt;, alloc::alloc::Global&gt; (834,501 samples, 2.40%)</title><rect x="62.1823%" y="261" width="2.3959%" height="15" fill="rgb(206,96,30)" fg:x="21658694" fg:w="834501"/><text x="62.4323%" y="271.50">pu..</text></g><g><title>write&lt;pulldown_cmark::tree::Node&lt;pulldown_cmark::parse::Item&gt;&gt; (834,501 samples, 2.40%)</title><rect x="62.1823%" y="245" width="2.3959%" height="15" fill="rgb(218,138,4)" fg:x="21658694" fg:w="834501"/><text x="62.4323%" y="255.50">wr..</text></g><g><title>next&lt;pulldown_cmark::parse::DefaultBrokenLinkCallback&gt; (1,332,422 samples, 3.83%)</title><rect x="64.5781%" y="293" width="3.8254%" height="15" fill="rgb(250,191,14)" fg:x="22493195" fg:w="1332422"/><text x="64.8281%" y="303.50">next..</text></g><g><title>convert (2,675,352 samples, 7.68%)</title><rect x="64.5781%" y="309" width="7.6810%" height="15" fill="rgb(239,60,40)" fg:x="22493195" fg:w="2675352"/><text x="64.8281%" y="319.50">convert</text></g><g><title>read_to_string&lt;&amp;std::path::Path&gt; (1,342,930 samples, 3.86%)</title><rect x="68.4035%" y="293" width="3.8556%" height="15" fill="rgb(206,27,48)" fg:x="23825617" fg:w="1342930"/><text x="68.6535%" y="303.50">read..</text></g><g><title>from_utf8 (1,342,930 samples, 3.86%)</title><rect x="68.4035%" y="277" width="3.8556%" height="15" fill="rgb(225,35,8)" fg:x="23825617" fg:w="1342930"/><text x="68.6535%" y="287.50">from..</text></g><g><title>run_utf8_validation (1,342,930 samples, 3.86%)</title><rect x="68.4035%" y="261" width="3.8556%" height="15" fill="rgb(250,213,24)" fg:x="23825617" fg:w="1342930"/><text x="68.6535%" y="271.50">run_..</text></g><g><title>finish_grow&lt;alloc::alloc::Global&gt; (2,259,328 samples, 6.49%)</title><rect x="72.2591%" y="309" width="6.4865%" height="15" fill="rgb(247,123,22)" fg:x="25168547" fg:w="2259328"/><text x="72.5091%" y="319.50">finish_g..</text></g><g><title>grow (2,259,328 samples, 6.49%)</title><rect x="72.2591%" y="293" width="6.4865%" height="15" fill="rgb(231,138,38)" fg:x="25168547" fg:w="2259328"/><text x="72.5091%" y="303.50">grow</text></g><g><title>grow_impl (2,259,328 samples, 6.49%)</title><rect x="72.2591%" y="277" width="6.4865%" height="15" fill="rgb(231,145,46)" fg:x="25168547" fg:w="2259328"/><text x="72.5091%" y="287.50">grow_impl</text></g><g><title>realloc (2,259,328 samples, 6.49%)</title><rect x="72.2591%" y="261" width="6.4865%" height="15" fill="rgb(251,118,11)" fg:x="25168547" fg:w="2259328"/><text x="72.5091%" y="271.50">realloc</text></g><g><title>__libc_realloc (2,259,328 samples, 6.49%)</title><rect x="72.2591%" y="245" width="6.4865%" height="15" fill="rgb(217,147,25)" fg:x="25168547" fg:w="2259328"/><text x="72.5091%" y="255.50">__libc_r..</text></g><g><title>_int_realloc (2,259,328 samples, 6.49%)</title><rect x="72.2591%" y="229" width="6.4865%" height="15" fill="rgb(247,81,37)" fg:x="25168547" fg:w="2259328"/><text x="72.5091%" y="239.50">_int_rea..</text></g><g><title>memcpy (2,259,328 samples, 6.49%)</title><rect x="72.2591%" y="213" width="6.4865%" height="15" fill="rgb(209,12,38)" fg:x="25168547" fg:w="2259328"/><text x="72.5091%" y="223.50">memcpy</text></g><g><title>__memmove_avx_unaligned_erms (2,259,328 samples, 6.49%)</title><rect x="72.2591%" y="197" width="6.4865%" height="15" fill="rgb(227,1,9)" fg:x="25168547" fg:w="2259328"/><text x="72.5091%" y="207.50">__memmov..</text></g><g><title>from_utf8 (501,504 samples, 1.44%)</title><rect x="78.7456%" y="309" width="1.4398%" height="15" fill="rgb(248,47,43)" fg:x="27427875" fg:w="501504"/><text x="78.9956%" y="319.50"></text></g><g><title>run_utf8_validation (501,504 samples, 1.44%)</title><rect x="78.7456%" y="293" width="1.4398%" height="15" fill="rgb(221,10,30)" fg:x="27427875" fg:w="501504"/><text x="78.9956%" y="303.50"></text></g><g><title>pop (2,905,448 samples, 8.34%)</title><rect x="80.1855%" y="309" width="8.3416%" height="15" fill="rgb(210,229,1)" fg:x="27929379" fg:w="2905448"/><text x="80.4355%" y="319.50">pop</text></g><g><title>surgerize_tight_list (2,905,448 samples, 8.34%)</title><rect x="80.1855%" y="293" width="8.3416%" height="15" fill="rgb(222,148,37)" fg:x="27929379" fg:w="2905448"/><text x="80.4355%" y="303.50">surgerize_ti..</text></g><g><title>scan_containers (501,504 samples, 1.44%)</title><rect x="88.5270%" y="309" width="1.4398%" height="15" fill="rgb(234,67,33)" fg:x="30834827" fg:w="501504"/><text x="88.7770%" y="319.50"></text></g><g><title>scan_paragraph_interrupt (1,381,463 samples, 3.97%)</title><rect x="89.9669%" y="309" width="3.9662%" height="15" fill="rgb(247,98,35)" fg:x="31336331" fg:w="1381463"/><text x="90.2169%" y="319.50">scan..</text></g><g><title>contains (1,381,463 samples, 3.97%)</title><rect x="89.9669%" y="293" width="3.9662%" height="15" fill="rgb(247,138,52)" fg:x="31336331" fg:w="1381463"/><text x="90.2169%" y="303.50">cont..</text></g><g><title>contains (1,381,463 samples, 3.97%)</title><rect x="89.9669%" y="277" width="3.9662%" height="15" fill="rgb(213,79,30)" fg:x="31336331" fg:w="1381463"/><text x="90.2169%" y="287.50">cont..</text></g><g><title>all (34,830,972 samples, 100%)</title><rect x="0.0000%" y="341" width="100.0000%" height="15" fill="rgb(246,177,23)" fg:x="0" fg:w="34830972"/><text x="0.2500%" y="351.50"></text></g><g><title>profile_md (34,830,972 samples, 100.00%)</title><rect x="0.0000%" y="325" width="100.0000%" height="15" fill="rgb(230,62,27)" fg:x="0" fg:w="34830972"/><text x="0.2500%" y="335.50">profile_md</text></g><g><title>scan_paragraph_interrupt_no_table (2,113,178 samples, 6.07%)</title><rect x="93.9330%" y="309" width="6.0670%" height="15" fill="rgb(216,154,8)" fg:x="32717794" fg:w="2113178"/><text x="94.1830%" y="319.50">scan_par..</text></g><g><title>scan_hrule (2,113,178 samples, 6.07%)</title><rect x="93.9330%" y="293" width="6.0670%" height="15" fill="rgb(244,35,45)" fg:x="32717794" fg:w="2113178"/><text x="94.1830%" y="303.50">scan_hru..</text></g></svg></svg>